import { Inter } from "next/font/google";

export const Configs = {
  apiKey: "apiKey",
};

export const Fonts = Inter({
  weight: ["400", "500", "600"],
  subsets: ["latin"],
  style: ["normal"],
  display: "swap",
});
